import { makeStyles } from '@material-ui/core/styles';


export const MainTheme = makeStyles((theme) =>({
  
  title: {
    flexGrow: 1
  },
  subTitle: {
    flexGrow: 1
  },
  
  

  
  }));

export default MainTheme;